/**
 * @file
 * StefaniniTema behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.stefaninitema = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
